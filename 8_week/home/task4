+4 если всё хорошо 
+1 не нашёл Employee e
+3 нет доступа к e.title, e.salary, и обоим сразу
+3 нет достура к e.name, e.surname и обоим сразу
+1 ушло в else, но нет доступа к e.name
+1 ушло в else, есть доступ к e.name, но нет к e.surname
+3 e.name,e.surname и вместе не добавляются в строку - функция не может вернуть 
+4 функции e.name(), e.surname() нельзя распечатать () 3 варианта в первом if и один в else
+3 e.title, e.salary с другими типами данных - не можем стравнить в if
Доп:
+2 std::string в обозначении возвращиемого типа - если было определено пространство имён, то лишнее в std::cout, если не было - нужно добавить в заголовке функиции

25 штук?



